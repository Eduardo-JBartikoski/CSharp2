namespace raiz_e_exponente;

class Program
{
    static void Main(string[] args)
    {
    
    int exponente = 2;
    double raiz_quadrada = Math.Sqrt(n);
    double potencia = Math.Pow(raiz_quadrada, exponente);
    if(potencia == n)
      {
      return true;
    }
    else
      {
      return false;
    }
    }
}
